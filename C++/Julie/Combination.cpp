#include <iostream>
#include <string>
using namespaces std;

class Combination {

	void Combination(string soFar, int rest) {
		if(rest == "")
			cout<<soFar<<endl;
		else {
			for(int i=0; i < soFar.length; i++) {
				next = soFar + rest[i];
				remaining = 
		}
	}
};
